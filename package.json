{
  "name": "dsh-enhance",
  "private": true,
  "type": "module",
  "description": "Native plugins that fill the gaps of the DeepSeek Harness: vision and direct-HTTP web tools, one composition row each.",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "build": "npm --prefix packages/dsh-vision run build && npm --prefix packages/dsh-native-web run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
